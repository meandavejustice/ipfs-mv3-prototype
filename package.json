{
  "name": "react-chrome-extension-mv3",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "copy-webpack-plugin": "^9.0.1",
    "ipfs-http-client": "^53.0.1",
    "is-fqdn": "^2.0.1",
    "is-ipfs": "^6.0.2",
    "lru-cache": "^6.0.0",
    "node-polyfill-webpack-plugin": "^1.1.4",
    "p-memoize": "^6.0.1",
    "p-queue": "^7.1.0",
    "rimraf": "^3.0.2",
    "web-ext": "^6.6.0",
    "webextension-polyfill": "^0.8.0",
    "webpack": "^5.61.0"
  },
  "scripts": {
    "prebuild": "rimraf build",
    "build": "webpack --mode development --devtool cheap-module-source-map ./addon/background.js --output-path ./build",
    "build:firefox": "web-ext build --source-dir ./build/"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ],
    "env": {
      "browser": true,
      "webextensions": true
    }
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "webpack-cli": "^4.9.1"
  }
}
